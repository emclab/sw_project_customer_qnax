<% if @view_in_config && @erb_code %>
    <%= render inline: @erb_code %>
<% else %>
  <%= simple_form_for @role_access_right do |f| %>
    <%= f.input :project_info_id, :as => :hidden, :input_html => {:value => @project_info.id} %>
    <%= f.input :user_role_id, :label => t('User Role'), :collection => @user_roles, :include_blank => true %>
    <%= f.input :action, :label => t('Action') %>
    <%= f.input :biz_form_id, :label => t('Biz Form'), :collection => @biz_forms, :include_blank => true, :placeholder => t('Choose form which the right is against...') %>
    <%= f.input :brief_note, :label => t('Brief Note'), :input_html => {:rows => 1} %>
    
    <%= link_to t('Back'), SUBURI + "/authentify/view_handler?index=0", :class => BUTTONS_CLS['action'] %>
    &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;        
    <%= f.button :submit, t('Save') , :class => BUTTONS_CLS['action'] %>
  <% end %>

<% end %>

